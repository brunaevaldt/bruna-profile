{
    "name": "bruna-profile",
    "version": "0.1.0",
    "private": true,
    "scripts": {
        "dev": "next dev",
        "build": "next build",
        "start": "next start",
        "export": "next export",
        "deploy": "rm -rf node_modules/.cache && rm -rf out && next build && next export && touch out/.nojekyll && cp CNAME out && gh-pages -d out -t true"
    },
    "dependencies": {
        "gh-pages": "^3.1.0",
        "next": "^10.0.0",
        "react": "16.13.1",
        "react-dom": "16.13.1"
    }
}
